# Journeys

This folder contains self-contained streamlit apps that, in a purpose
similar to Jupyter notebooks, each tell a story about the underlying data
of the Westie Music Database, or about the way we process it.

The apps can be run by executing `streamlit run journeys.py` in the root
directory of the repository.
